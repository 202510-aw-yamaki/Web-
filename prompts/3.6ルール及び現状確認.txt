レスポンシブ対応：
- モバイル: 〜767px（デフォルト）
- タブレット: 768px〜1023px
- PC: 1024px以上
を基準とします。

＜ページ内ルール＞
- モバイル	：『`body.home-page` は `100dvh` 固定・`overflow:hidden`。`page-wrap` は「パンくず+ヘッダー」分を上余白、固定フッター分を下余白として確保し、`main` を縦flex化。`hero` は固定高、`map-shell` が残り領域を占有する。』
- タブレット	：『モバイルの基本レイアウトを維持。`japan-map-area` の最大幅を 700px に制約して地図比率崩れを抑制する。』
- PC		：『モバイル/タブレットの縦構成を維持しつつ、地図ホットスポット表現やナビ表示をPC向けに切替。`japan-map-area` 最大幅は 760px。』

※ヘッダー、メインエリア、フッターがそれぞれどのようなルールになっているのかを『』内に記述してください。

＜ヘッダールール＞
- モバイル	：『`top-breadcrumb`（24px）+ `site-header`（72px）を固定表示。右上は提灯ボタン（`#menuToggle`）で、押下時に `mobile-menu.is-open` を付与して開閉する。』
- タブレット	：『モバイルと同じ。提灯ボタン+モバイルメニュー運用を継続。』
- PC		：『`@media (min-width:1024px)` で `mobile-lantern/mobile-menu/#menuToggle` を非表示、`pc-lanterns` を表示（`max-width:72%`）。』
※ヘッダー内のルールはどのようなルールになっているのかを『』内に記述してください。

＜メインエリア内ルール＞
- モバイル	：『`hero` は白背景カード（角丸16px）。`hero-top` は見出しとCTAを横並び（見出し側は省略表示あり）。地図領域は `map-shell` 内で可変し、`japan-map-area` は `aspect-ratio: 816/768`。』
- タブレット	：『モバイル構成を維持。地図が過度に横長化しないよう幅制御のみ追加。』
- PC		：『トップ地図の地方ホットスポットは「数字バッジ」から「画像アイコン表示」に切替。POIモーダル本文は2カラム配置へ切替。』
※メインエリア内のルールはどのようなルールになっているのかを『』内に記述してください。

＜フッタールール＞
- モバイル	：『`fixed-nav` を画面下固定。Info/Legal/Support の3アコーディオンを等幅表示し、展開内容は上方向にポップアップ。』
- タブレット	：『モバイルと同じ。横並び3カラム維持。』
- PC		：『基本は同一運用。固定フッターの実測高さをJSで `--fixed-nav-content-h` に反映し、メインの見切れを防止。』
※フッター内のルールはどのようなルールになっているのかを『』内に記述してください。

＜モーダル内ルール＞
- モバイル	：『`dialog.poi-modal` は `width:90vw` / `max-height:88dvh`。本文は1カラムで、テキスト→画像を縦積み。CTAはモーダル上部中央。』
- タブレット	：『モバイルの1カラム構成を継続し、可読性を優先。』
- PC		：『`poi-block` を2カラム化（本文 + 画像）。`image-right/image-left` クラスで左右配置を切替。タイトル内の `<br>` は非表示化。』
※モーダル内での文章及び画像配置がどのようなルールになっているのかを『』内に記述してください。

※ここでいうルールとはサイズの指定や制約などを指します。

＜ＣＳＳ状況＞
共通項目	：『色変数、ヘッダー/ナビ、hero、地図コンテナ、モーダル、固定フッター、レスポンシブ切替は `assets/styles.css` に集約されており、全地方ページで共通利用できている。』
個別項目	：『POI位置指定（例: `.map-poi-btn-kanto`）や地方ホットスポット位置（`.spot-kanto` など）は個別クラスで管理。ページ追加時に「HTMLへボタン追加 + CSSへ位置追加」が必要。』
※最終的には全ページでほぼ共通化し、追加の要件（九州、中国地方や各地域に特産品が増えた時など）が起きた時に追加しやすいようにしたいです。ここをこうすれば改善できるなどあれば下記『』内に記述をお願いします。
改善案		：『POI座標を「CSS個別クラス」から「HTMLの data属性（例: data-x/data-y）」へ寄せ、共通 `.map-poi-btn` で座標反映する方式に変更すると保守性が上がる。加えて、`regions.json` のような定義ファイル（地域名、画像、モーダルID、座標）を作ると、中国/九州の追加実装や特産品増加時の更新コストを下げられる。』


＜サイト構成＞
現状のサイト構成を日本語での解説をつけて記述して以下の『』内に記述してください。
『
- ルート `index.html`：日本地図トップ。地方ホットスポットから各地域ページへ遷移。
- 地方ページ：`hokkaido/tohoku/kanto/chubu/kinki/shikoku/chugoku/kyushu/index.html`。
  - 北海道/東北/関東/中部/近畿/四国：地図上POIボタン + モーダル詳細あり。
  - 中国/九州：現時点では地図表示中心（POI/モーダル未実装）。
- 共通資産：`assets/styles.css`（見た目）と `assets/script.js`（提灯メニュー、固定ナビ高さ同期、モーダル開閉）。
- 画像資産：`image/` 配下に地方別・特産品別で格納。
- 法務ページ：`legal/privacy.html`, `legal/terms.html`。
- 企画メモ：`prompts/` 配下（構成案、ワイヤー、草案）。
』


＜各ページの記述＞
どこに何を入れれば反映されるか、特にCSSにおいてPOI位置の設定をどうするかなどをページ内に記述していければいいなと考えています。（保守性、更新のやりやすさなど）
そのあたりの不足部分、及び提案があれば下記の『』内に記述してください。
不足部分	：『1) 「新しいPOI追加時の手順」が文書化されていない。2) 予約導線（リンク先・文言・`target`）の統一ルールが未定義。3) 中国/九州を準備中扱いにするか実装を進めるかの運用基準が未整理。4) POI位置がCSSに分散し、対応表がない。』
提案		：『`prompts` に「運用手順書」を1枚追加する。内容は①地域ページへのPOI追加手順（HTML/CSS/画像命名）②モーダル本文テンプレート ③CTA統一規約 ④完了チェックリスト（モバイル/タブレット/PCで位置確認）を固定化。POI位置は「%指定の根拠（基準画像サイズ・確認解像度）」も併記し、更新者が再現できる状態にする。』
